// local
// import { wrapHandler } from "./handlers";

describe("wrapHandler", () => {
  test.todo("Returns non-function as static data");
  test.todo("Returns serialised error on sync throw");
  test.todo("Returns serialised error on async throw");
  test.todo("Returns data from sync function");
  test.todo("Returns data from async function");
});
